services:
  frontend:
    build:
      context: .
      args:
        RESEND_API_KEY: "resend-api-key"
    image: portfolio_frontend
    container_name: portfolio_frontend
    ports:
      - "4000:3000"
    networks:
      - nginx_network

networks:
  nginx_network:
    driver: bridge
