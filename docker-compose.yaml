services:
  frontend:
    build:
      context: .
    image: frontend
    container_name: frontend
    ports:
      - "3000:3000"
    networks:
      - nginx_network

networks:
  nginx_network:
    driver: bridge
