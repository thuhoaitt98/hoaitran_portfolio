services:
  frontend:
    build:
      context: .
    image: portfolio_frontend
    container_name: portfolio_frontend
    ports:
      - "4000:3000"
    networks:
      - nginx_network

networks:
  nginx_network:
    driver: bridge
